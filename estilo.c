#include <stdio.h>
int main(){
    int contador;
    printf("digite um contador:  ");
    scanf("%d", &contador);
    while (contador >= 1){
        printf("%d\n", contador);
        contador--;
    }
    printf("Lançamento", contador = 1);
    
    return 0;
}